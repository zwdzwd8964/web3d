{
  "name": "@w3/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Golden-path browser tests (G0-1). Separate package so Playwright's browser download is not a dependency of building the engine.",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@gltf-transform/core": "4.4.2",
    "@playwright/test": "1.62.0",
    "@w3/storage": "workspace:*"
  }
}
