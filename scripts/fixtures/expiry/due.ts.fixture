// 逐字复刻 ADR-0022:79 的实例。它的 <条款> 段是 `§3`，不是一条 C 编号——
// 把 <条款> 收成 /^C[1-9]$/ 的解析器会拒掉它，而「解析不出来 = 红」，
// 于是守卫会在上线当天把 ADR-0022 判成违规。
export function benchmark(): void {
  // CONSTITUTION-EXCEPTION: §3 · ADR-0022 · 到期 v1.0
}
