// 版本号成形但不在阶梯上。**这份夹具是变异 ③ 的替身**：
// 卡面原来的变异（改成字符串比较）在这个阶梯上不可能红——七级恰好字典序有序，
// 两种比较对任何合法级别同解。`v1.10` 才是字典序会判错的那个，而它压根不在阶梯上，
// 所以真正该守的是「成员校验」，不是「比较方式」。
export function later(): void {
  // CONSTITUTION-EXCEPTION: C6 · ADR-0033 · 到期 v1.10
}
