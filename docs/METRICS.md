# 指标记录 · METRICS

NORTH_STAR §7 要求每个版本收尾把指标表填一遍。**数值不重要，趋势重要。**

---

## 快照 · 2026-07-30 · 底座层完成（M0 / M1 / M2 核心 / M4）

对应提交 `e1efc1f`。编辑器与播放器尚未开工，因此涉及它们的指标标注为"未适用"而非留空。

| 指标 | v0 目标 | 本次实测 | 说明 |
|---|---|---|---|
| `@w3/core` 中框架相关 import 数 | 0 | **0** | 由 `check-core-purity.mjs` 机械保证；已用探针实证它真能拦住 `import React` |
| 新增一个动作类型所需改动文件数 | ≤ 3 | **2** | `actions/<group>.ts`（定义 + 加入 BUILTIN_ACTIONS 数组，同一文件）+ `actions.test.ts`。若该动作需要新的 RuntimeContext 能力则 +2（`eca/types.ts` 与两个 Runtime 实现），仍在 3 以内的精神范围但已触及上限 |
| 已注册动作的单测覆盖 | 100% | **100%（13/13）** | G0-5 门槛：`actions.test.ts` 遍历注册表比对测试清单，注册了却没写测试即 fail |
| Player 构建产物体积（gzip） | ≤ 400 KB | **未适用** | Player 未开工（T-101） |
| 从零构建耗时 | 记录基线 | **2,325 ms** | 已连网，`rm -rf packages/*/dist && pnpm build`。**断网构建尚未实测**（C6 的这一半仍未验证） |
| 分诊落在 Q4 的需求占比 | —（v1 起考核） | — | |
| core 复用率 | —（v2 起考核） | — | |

### 补充实测

| 项 | 数值 |
|---|---|
| 源码行数（不含测试） | 8,526 |
| 测试行数 | 6,458 |
| 测试数 | 551（schema 144 / storage 67 / core 340），0 跳过 |
| `@w3/schema` 语句覆盖 | 94.3%（门槛 90%） |
| dist 体积（未压缩） | schema 462 KB · storage 94 KB · core 712 KB |
| ADR 数 | 11 |

### 晋级门槛核对（NORTH_STAR §3）

| # | 门槛 | 状态 |
|---|---|---|
| G0-1 | 黄金路径 12 步 E2E 全绿 | ❌ 未开工（需编辑器） |
| G0-2 | `pnpm check:constitution` 全绿 | ✅ **通过**（C2/C8 · C7 · 依赖方向 · C6） |
| G0-3 | Schema 迁移回归：历史 fixture 全部可打开 | ✅ **通过** |
| G0-4 | 编辑器预览 / 播放器 状态轨迹一致性 | ❌ 未开工（需 Player） |
| G0-5 | 已注册动作 100% 有无 GPU 单测 | ✅ **通过**（13/13） |
| G0-6 | 资产二次上传重映射报告正确（含 orphan 识别） | ✅ **通过**（五种分类各有单测，含"孤儿未被删除"断言） |
| G0-7 | benchmark 在目标机器实测 | ❌ 未执行 |

**7 项中 4 项通过。** 未通过的三项全部阻塞于尚未开工的编辑器与播放器，不是已知缺陷。

---

## 趋势观察点

下次填表时重点看这三条，它们比绝对值更能说明底座在变好还是变坏：

1. **新增动作所需改动文件数**是否仍是 2。一旦涨到 4，说明 C5 的抽象开始漏，通常表现为
   "这个动作需要执行器知道点什么"。
2. **`fullRebuildCount`**（D1 兜底计数）在 E2E 里是否仍为 0。它从 0 变成任何正数，
   都意味着有一类编辑操作走了全量重建路径，而没人注意到。
3. **断网构建**一旦实测过，之后每版都要跑。C6 的检查脚本只查产物里的外链，
   查不出"构建过程需要联网"。
