# T-280 · 目标机器的 bench 报告放这里。
#
# 命名：<档位>-<代号>.json  例如 M2-车间工控机.json
# 档位：M1 / M2 / M3（见 ../BENCHMARK.md「建议至少测三种配置」）
#
# 放进来之后跑 `node scripts/apply-bench-report.mjs`，附件A §7 由它生成。
# 软件渲染下导出的报告会被拒绝——那不是疏漏，是 §7 前提 3。
