# v1 点火指令（交给写代码的 agent 的第一条指令）

> **这份是 v1 启动指令的唯一权威。** [MVP_V1_进化规划.md](MVP_V1_进化规划.md) §9 的同名代码块
> 写于文档定稿当天，**已被本文取代**（它没有覆盖仓库当时的实际状态，也没有协作节奏三条）。
> §9 的原文按惯例保留留痕，**不再修改、不再引用**——同一份内容存在两处、漂移是时间问题，
> 这正是 `docs/CLAUDE.md` 与根 `CLAUDE.md` 分叉那次的形状。
>
> 本文有变更时**只改本文**。协作节奏的权威在根 [`CLAUDE.md`](../CLAUDE.md) 的「协作节奏」一节，
> 本文只做摘要指路，不重抄细则。

把下面整段原样交给写代码的 agent 作为第一条指令。

---

```
你要实现的是 Web 3D 工具引擎的 v1 版本。v1 经 ADR-0020 拆成三级台阶：
v1.0「地基与表现力」/ v1.2「编排与复用」/ v1.5「合同交付」。你现在做的是 v1.0。

底座（v0）与表现力第一轮（v0.5）已完成：45 张卡、1308 条测试、Player gzip 243.8/400 KB、
16 个注册动作、40 条 ADR。v1 全书 199 张卡 / 222.7 人日（v1.0 100 张 / 111.0 人日 / ≈129 小时
agent 时钟），排期真值以 TASK_BACKLOG_V1.md 附录 C 为准。

【开工授权】产品负责人已于 2026-08-03 拍板 P-1 ~ P-20 二十项，全部批准，留痕在规划文末一节。
其中 P-2 采纳 ADR-0022 选项二：G0.5-8（目标机器 benchmark）由「v0.5 晋级 v1 的入口门槛」
改挂为 v1.0 的【出口】门槛（规划 §7.2 人工项 H1）。v1.0 因此即刻合法开工——
但 v1.0 出口 = G1.0-1 ~ G1.0-22 全绿【加】H1 通过，两者缺一不可。
连带后果：像素比封顶 2 / 剖切平面上限 3 / 描边预设上限 2 / fog.near=10,far=100
这四个默认值现在【是拍的不是测的】，碰到它们时不要当成实测结论引用，也不要据此推导别的数字。

═══════════════════════════════════════════════════════════════════
【最先读这一段：我要的协作节奏】权威在根 CLAUDE.md「协作节奏」一节，这里是摘要
═══════════════════════════════════════════════════════════════════

一 · 步子要大。默认【一轮连续做 10 ~ 15 张卡】，不要做完一张就回来等指令。
     卡与卡之间不请示，自测绿了就继续下一张，汇报攒到批次末尾。
     中途只有两类情况允许停：① 触发了下面「必须停下来问人」八条中的任何一条；
     ② 台账写死的阀门（T-206 之后 · T-225 单卡波次 · 里程碑收尾）。
     其余问题【自己判断、自己往下走】，把判断写进提交信息或 ADR，批次末尾一并汇报。
     某张卡卡住时不要停整批：跳过它，继续做批次里不依赖它的卡，把跳过的卡与原因记进批次报告。

二 · 不确定时给我出【选择题】，不要开放式提问。2 ~ 4 个具体选项，每个写清：
     它具体做什么（落到文件/字段/命令级）· 代价（人日 / 风险 / 以后要还的债）· 你推荐哪个及为什么。
     一次可以出多道题。
     ⚠ 技术裁决你自己拍板，别做成选择题。出给我的只有三类：
     改宪法或版本阶梯的 · 影响合同措辞的 · 需要外部资源的。这三类要把两个选项的代价都写全，
     明确不代我选。

三 · 每批收尾给下一批的【预告】，固定四行，然后停下来等我一句话：
       下一批打算做：T-2xx ~ T-2yy（N 张卡，属 M1x「里程碑名」）
       主要内容：一句话
       工作量预估：合计 X.X 人日 ≈ Y 小时 agent 时钟（换算见台账附录 C.3）
       风险点：这批里最可能超预估 / 最可能假绿 / 最可能要 ADR 的是哪一张
     我会回「精细」或「快速推进」：
       精细     = 变异检验全跑、E2E 补上、边界逐条列、该写的 ADR 都写，慢没关系
       快速推进 = 走 DoD 最小闭环（实现 + 自测绿 + 只跑卡面点名的那处变异 + 台账回填），
                  可延后的登记进 IMPL_NOTES
       我不说话或只说「继续」= 按你预告的原样做；地基卡与 schema 卡默认按精细，其余按快速推进
     ⚠「快速推进」不是降低门槛：check:constitution / typecheck / lint / 卡面自测 /
     变异检验登记 / 台账回填这六项任何模式下都不许省。它省的是广度，不是下限。

═══════════════════════════════════════════════════════════════════

【开工前必读，按顺序】
1. docs/NORTH_STAR.md          宪法。九条约束任何情况下不得违反。§3 版本阶梯与 §7 指标表
                               本版有改动（Player 预算 600→400 KB），以 ADR-0020 为准。
2. docs/MVP_V1_进化规划.md      做什么/不做什么（§1.2 Out of Scope · §1.3 灰区裁决）、
                               黄金路径 III（§2.1）、架构影响面 §3、【§4 是逐字规范不是建议】、
                               设计决策 D21–D40、门槛 §7.1/§7.2。
                               （§9 的启动指令已被本文取代，不要照它执行。）
3. docs/SCHEMA_SPEC.md         现行文档模型。v1 增量一律以规划 §4 为准，
                               schema v3 落地后按 §4.5 的【独立编号回写卡】回写此文件。
4. docs/ECA_SPEC.md            规则引擎。新增动作走 §10 三文件法。
                               注意 §5.1 与 §6 两处规范滞后于代码（规划 §1.0(c)），改规范不改代码。
5. CLAUDE.md（仓库根目录）      工程铁律 · DoD · 协作节奏。每次提交前对照。
                               ⚠ docs/CLAUDE.md 是已停止维护的历史副本，口径停在 v0.5，
                               不许引用、不许修改。
6. docs/IMPL_NOTES.md §4       历次对抗式审查。E18 那节的三条教训直接决定你怎么写测试。
7. docs/TASK_BACKLOG_V1.md     任务卡。§0 本版新纪律八条 → §1 波次表 → 从 T-200 起领卡。

【仓库现状，与文档不一致的四处，开工前先处理】
(a) 整套 v1 文档尚未入库：MVP_V1_进化规划.md · TASK_BACKLOG_V1.md · V1_KICKOFF.md ·
    ADR-0020~0026 · 0030 · 0031 全是 untracked；NORTH_STAR.md · CLAUDE.md · 技术方案 · 附件A
    有未提交改动。
    【第零步】：先把这批规划文档单独提交为一个 docs 提交（不带 T- 卡号，提交信息写清这是
    v1 规划集合入库），然后再开 T-200。混进第一张实现卡会毁掉「一卡一提交」的二分定位价值。
    docs.zip 是打包快照，不要提交。
(b) docs/MUTATIONS.md 与 docs/DEAD_EXPORTS_ALLOWLIST.md 都【不存在】。它们的脚本锁分别由
    T-297 / T-205 交付，但【登记从 T-200 第一次变异就开始】。所以 T-200 收尾时你要
    手工建 docs/MUTATIONS.md，表头逐字用七列：卡号 / 编号 / 操作 / 期望 / 实际 /
    若绿属哪一类 / 处置。T-297 只是把它锁上，不是它的起点。
(c) ADR 编号 0027 / 0028 / 0029 是【预留空洞】，分别归 T-202（IndexedDB 版本阶梯）、
    T-203（RefKind 注册表化）、T-204（变量变化的跨 await 循环防线）。0030 / 0031 已被占用。
    不许拿这三个号写别的东西，也不许因为「看起来断号了」去重排。
(d) 仓库里已存在 untracked 的 Dockerfile / railway.toml / deploy/nginx.conf.template / .dockerignore，
    而规划 §1.1 与 §7.1 按「这三个路径 git log 为空」写的，它们的正式交付卡是 v1.5 的
    T-221 / T-293 / T-419。【不要把它们当成 v1 交付物，也不要顺手删】——在 T-221 开工那天
    以它们为起点，在卡面上如实登记「已有草稿，来源不明，本卡接管」。

【已核实为真的命令】pnpm install / dev / dev:player / build / typecheck / lint / test /
test:parity / test:e2e / check:constitution / size / verify / verify:full 全部存在。
`pnpm size-limit` 【不存在】，四份文档写错了，由 T-207 一次改完。
scripts/ 下起草时只有 9 个脚本。**T-205 / T-207 落地后已增至 13 个**（含 scripts/lib/），
其中 check-dead-exports.mjs 与 check-docs.mjs 已存在并已挂进 pnpm check:constitution / pnpm verify。
check-expiry.mjs · check-mutations.mjs · milestone-close.mjs · check-backlog-conflicts.mjs ·
apply-bench-report.mjs · pack-offline.mjs · gen-appendix-c.mjs 仍不存在，各有交付卡，别假设它们能跑。

【工作方式：沿用 v0/v0.5，外加五条新纪律】
- 一次一张卡地做，但【连着做完一整批再汇报】（见上文协作节奏一）。每张卡做完跑该卡的
  「自测」命令，在 TASK_BACKLOG_V1.md 里把状态改 [x] 并回填实际耗时（小时），
  同时跑一次 pnpm size 把 gzip delta 记进提交信息。
- 规范没写清楚 → 先在 docs/adr/ 写 ADR 再实现，不许静默做假设。ADR 的「代价」与「撤销条件」
  两栏不许留空。写 ADR 不是停工事由，写完继续做，批次末尾汇报。
- 【纪律 1 · 变异检验】新增的每条测试附一次变异检验：把被测行为故意改坏，测试必须转红。
  每一次——转红的和没转红的——都登记进 docs/MUTATIONS.md 的七列表，不是写进提交信息。
  不转红时【不许直接补断言了事】，先判断是哪一种：
  (a) 测试没测到东西 → 补断言；(b) 冗余机制互相掩护 → 删掉多余那条，或把断言收紧到能分开的
  粒度（如报错措辞）；(c) 被测代码本来就是对的 → 记录并放过。
  预期本版约 377 次变异里近百次是绿的（v0.5 实测 31 次里 8 次绿）。这是排期事实，不是你写得差，
  【也不是停下来问人的理由】。每张卡的「变异检验」栏已经点出这张卡最可能假绿的那一处，先跑它。
- 【纪律 2 · 能力链认领】你交付的每一条能力，提交前问一句「用户在编辑器里从哪里点得到它？」
  答不上来 = 这条链没做完。往黄金路径 III 的能力入口体检表里加一行并给出选择器；加不出来就停下来说。
  卡面上凡出现「面板 / 入口 / 按钮 / 对话框」的，那一环必须在本卡内完成，不许推给「后面某张 UI 卡」。
- 【纪律 3 · 谁指向它】新增任何被引用对象（新集合 / 新 id 前缀 / 新 ref 种类）之前，先写出五个
  遍历面：collectAllIds · checkIntegrity 的 I1 集合表 · buildIndex 的出边 · referencedHashes ·
  编辑器快照回滚的 replaceInPlace。每一行都要有一条测试；已核实不需要改的也要写「已核实不需要」，
  空着等于没查。
- 【纪律 4 · 断言真源】核心断言一律读运行时真源，不读文档：graph.objectFor(id).position /
  renderer.clippingPlanes / composer.passes / SceneRuntime.variables。编辑态下文档值与运行时值
  经常恰好相等，这正是它危险的原因。前置断言要断言形状（toBe('SpotLight')、toHaveLength(1)），
  不要用 not.toBeNull() 这类对 undefined 也成立的写法。对称性测试两头都要断言——只比较两端的
  测试对「中间什么都没发生」完全无感（E18 教训 2）。新建顺序 id 工厂会造出与已有数据相同的 id
  （E18 教训 3，同一批卡踩了两次）。
- 【纪律 5 · 接上去】写完一个模块，最后一步是把它接进生产路径。check-dead-exports.mjs 会拦你，
  但别等它拦——这个仓库里「完整实现 + 有单测 + 零生产调用者」同一形状已复发 14 次。

【必须停下来问人，不许自己拍板】——这八条是「步子迈大」的唯一例外，其余一律往下走
1. 需要改 SCHEMA_SPEC / ECA_SPEC 的字段定义；
2. 【任何超出规划 §4 冻结清单的字段 / 动作 / 事件改动】——新增字段、改名、改类型、改默认值、
   加一种事件或动作，只要不在 §4 的逐字 zod 清单里，一律停。schema 在 v1 只 bump 一次（2→3，
   在 v1.0 内完成），§4 同时冻住了 v1.2 与 v1.5 才消费的全部字段。
   【开工后发现漏字段 → 登记 v2，不追加】（ADR-0020 决定第 1 条）。自己补一个字段进去，
   就是把唯一一次 bump 变成两次；
3. 需要改 executor.ts 或 engine.ts。本版对这两个文件各有且只有一次合法改动：T-203（executor）
   与 T-204（engine），【两条 ADR 今天都不存在，由这两张卡各自先写 ADR 再动手】
   （T-203 → ADR-0028 · T-204 → ADR-0029）。除此之外这两个文件的 diff 必须为 0；
4. 需要引入规划里没列的第三方依赖（已批准的五个见 ADR-0030，批准 ≠ 安装，各自在引入卡当天才
   写进 package.json 与 lockfile）；
5. 某条宪法 C1–C9 挡住了唯一可行的实现路径；
6. 卡的验收标准与 SPEC 冲突；
7. 一张卡实际耗时超出预估 2 倍以上；
8. Parity 测试写不出来或持续不过——它意味着编辑器与播放器已经分叉（C3），这时候继续加功能
   只会让返工面积变大。
停下来的时候【按协作节奏二出选择题】，不要开放式提问。
不确定「该不该做」→ 回规划 §1.2 Out of Scope；三份清单（v1 / v0.5 / v0）都查不到 →
走 NORTH_STAR §4 分诊四问，把结论写在任务卡上——这一步是你自己走完的，不用问我。

【起手顺序与两个阀门】
第零步：把 v1 规划文档集合入库（见上文 (a)）。
第一批（建议 12 张，按 W0 + W1 取）：T-200 ~ T-206 六张地基卡 + 冻结裁决表，
  然后 T-207 · T-217 · T-222 · T-224 · T-208。它们之间有序：
  T-201 先落集合注册表，再动 CURRENT_VERSION；T-202 先升 DB_VERSION，再开崩溃恢复；
  T-203 与 T-204 都是 ADR 先行；T-208 排在 T-207 之后（共享 package.json）。
★ 阀门一：【T-206 完成后停下来汇报，等确认再开 T-225 那张 schema bump 卡。】
  理由比 v0 的 T-001、v0.5 的 T-120 更硬：v1 的冻结清单是十三份并行设计汇总的，交叉核对
  已实测出 14 处冲突、其中 11 条 blocker，而「单方面出清单」这种做法已被证明会漏掉两个领域的
  全部字段。schema v3 的字段形状错了，后面九个里程碑全建在错的地基上，且这是唯一无法靠单测
  发现的错误类型。
★ 阀门二：T-200 与 T-235 交付时必须【同步交付接缝清单】（空实现 + throw '未接线' + 一条
  断言它们全部被接线的清单测试）。这两处是全 v1 并行度的两个阀门；不交付，scene-runtime 与
  eca/types 两条串行链会把关键路径从 16 波拉到 25 波以上，墙钟翻倍。

【为什么六张地基卡排在一切功能卡之前】它们中的每一条，都对应一次「多个领域同时踩空、而各自的
回归测试全绿」的实证：五份设计的无 GPU 单测建在一条不存在的渲染器注入缝上；三份设计把
DB_VERSION 都写成 2 且三条回归测试都会绿。地基卡不是「先做基础设施」的惯性，是已经量到的返工面积。
```

---

## 批次报告模板（每批收尾照这个写）

```
■ 本批完成：T-2xx ~ T-2yy，N 张卡，实际 X.X 小时（预估 Y.Y 人日）
  逐卡一行：卡号 · 一句话结果 · 实际耗时 · gzip delta

■ 变异检验：本批 M 次，其中 K 次不转红
  逐条：卡号/编号 · 属 (a)(b)(c) 哪一类 · 怎么处置的（详表已进 docs/MUTATIONS.md）

■ 自行裁决的（没来问你，理由写在这）：
  · …（每条给出落点：提交信息 / ADR-00xx / IMPL_NOTES 哪一节）

■ 跳过或未完成的：
  · 卡号 · 卡在哪 · 需要什么才能继续

■ 需要你定的（选择题，若无则写「无」）：
  Q1. 一句话说清要定什么
      A) 具体做什么（落到文件/字段/命令级） · 代价（人日 / 风险 / 以后要还的债）
      B) 具体做什么 · 代价
      推荐 A，因为 …

■ 下一批打算做：T-2xx ~ T-2yy（N 张卡，属 M1x「里程碑名」）
  主要内容：
  工作量预估：X.X 人日 ≈ Y 小时 agent 时钟
  风险点：
```
