# 性能实测 · BENCHMARK

> 怎么在**客户的机器上**出一份可以进合同的性能报告。
> 对应任务卡 T-110 · 配套 [附件A_数字资产规范_草案.md](附件A_数字资产规范_草案.md)

---

## 为什么必须在客户机器上测

我们这边测出来的数字，对验收没有意义。原因有三条，每条都足以让数据作废：

1. **显卡差一个数量级。** 集成显卡与独立显卡在同一场景上能差 5–10 倍；
2. **硬件加速可能是关的。** 企业统一镜像里禁用 GPU 加速并不罕见，届时浏览器退回软件
   渲染（SwiftShader），帧率数据完全不可比；
3. **浏览器内核可能偏旧。** v0 需要 WebGL 2（[ADR-0013](adr/0013-v0-不支持-webgl1.md)）。

所以 benchmark 做成了一个**独立单页**：拖进去一个 `.w3p` 就能出报告，不需要装编辑器、
不需要联网、不需要我们在场。

---

## 怎么用

1. 把 `packages/player/dist/` 整个目录拷到客户机器上（或部署到内网任意静态服务器）；
2. 用支持 WebGL 2 的浏览器打开 `bench.html`；
3. 把要测的 `.w3p` 拖进页面（或点「选择 .w3p」）；
4. 等约 6 秒——期间相机自动环绕，这是刻意的：静止画面会让驱动跳过工作，测出一个
   用户永远体验不到的帧率；
5. 点「复制为 Markdown」，把结果贴进工单；
6. 再点「下载 JSON」，把那个文件放进 `docs/bench-reports/`——
   [附件A](附件A_数字资产规范_草案.md) §7 由脚本从它回填，**不是从 Markdown 里抄**。

前 30 帧会被丢弃：着色器编译和贴图上传都发生在那里，与稳态性能无关。

---

## 报告里的每一项是什么

| 指标 | 含义 | 阈值来源 |
|---|---|---|
| **平均帧率** | 连续渲染时的帧率 | 经验值：45 fps 黄灯 / 25 fps 红灯。**尚无目标机器实测** |
| **P95 帧时间** | 每 20 帧里最慢的那一帧 | 经验值：33 ms 黄灯 / 66 ms 红灯 |
| **最慢单帧** | 仅供参考 | — |
| **三角面数** | 一帧实际提交的三角形 | `DEFAULT_POLICY.maxTriangles` = 300,000 |
| **Draw call** | 一帧的绘制批次 | 经验值 1,200。**无实测来源**，报告里已注明 |
| **贴图显存（估算）** | 按 RGBA8 + 完整 mip 链推算 | `DEFAULT_POLICY.maxTextureBytes` = 128 MB |
| **几何体 / 贴图 / 着色器** | 常驻显存的对象数量 | 无阈值，用于横向比较两个模型 |
| **承载上限（逐级加载）** | 把整个场景复制 ×1/×2/×4/×8 同时渲染，加到跌破帧率为止 | 帧率阈值同上。压的是 drawcall 与顶点吞吐，**不压显存**（[ADR-0016](adr/0016-逐级加载压力测试的含义.md)） |
| **首屏 · 解包 / 建场景 / 首帧 / 合计** · v1.0 | 打开一个包到画出第一帧，分三段 | **无阈值来源**，见下 |
| **动态灯上限（无阴影）** · v0.5 | 0/1/4/8 盏动态灯，不投影 | 帧率阈值同上 |
| **动态灯上限（low / medium / high 阴影）** · v1.0 | 同上，但开阴影，**三档各一行** | 帧率阈值同上。**与上一行分开报**，理由见下 |
| **建议出厂默认阴影档** · v1.0 | 这台机器上默认该开哪一档 | 取「还带得动 ≥1 盏投影灯」的最高一档 |
| **阴影贴图显存（估算）** · v1.0 | `投影灯数 × 贴图边长² × 4 字节` | 边长读 `shadowMapSizeFor`（512 / 1024 / 2048） |
| **剖切切换首帧代价（开 / 关）** · v1.0 | 开关一次剖切之后的**第一帧** | **无阈值来源**，见下 |
| **剖切引起的 shader 重编译** · v1.0 | 切换前后 `renderer.info.programs` 的差 | 无阈值，是上一行的成因 |
| **爆炸进行中帧率** · v1.0 | 爆炸动画**进行中**的稳态帧率 | 帧率阈值同上 |

### 关于「平均帧率」和「P95 帧时间」

只看平均帧率会漏掉最影响体感的问题。一个场景以 60 fps 运行、每秒卡一下 400 ms，
平均下来仍然好看，而用户描述的是「时不时卡一下」。**那种卡顿只出现在 P95 和最慢单帧里。**

### 关于两个「动态灯上限」为什么分开报（v0.5 · T-174）

灯数与阴影是**两种不同的代价**，混成一个数字会让人得出「灯很慢」然后干脆不用灯：

- **多一盏灯**：多一份逐像素的着色计算，代价**平滑上升**；
- **多一盏投影灯**：多一整遍深度 pass，代价**阶梯式上升**。

同一台机器上，「能开几盏灯」和「能开几盏带阴影的灯」经常差好几倍——而这个倍数，
正是排一个场景时最需要知道的数。所以报告里是两行，不是一行。

### 关于三档阴影为什么必须都测（v1.0 · T-279）

到 T-279 之前，bench 页测的是 `['off','medium','high']`，而 `ShadowQualitySchema` 的
三档是 **low / medium / high**。也就是说 **`low` 是一档用户选得到、报告里永远没有数的
设置**——而它恰好是低配机器上唯一可能跑得动的那一档。

**测的档必须等于文档能表达的档。** 现在 `SHADOW_MODES` 从 `SHADOW_QUALITIES` 派生，
schema 加一档它自己就跟着加。

结论行也从两条变成四条（off + 三档）。少了第三个数，「出厂默认该开哪一档」这个问题
在报告里根本**没有可比较的对象**——这是遗留决议 S3 一直悬着的原因。

### 关于「建议出厂默认阴影档」（v1.0 · T-279）

规则一句话：**取「至少还带得动 1 盏投影灯跑在 45 fps 以上」的最高一档；一档都不行就
建议关闭。**

门槛是 1 盏而不是 4 盏，因为默认档回答的是「开还是不开」，而一盏投影灯是「阴影这个
功能开着」的最低成立条件。带不动 1 盏就该默认关——给用户一个开着但一开就掉帧的默认值，
比默认关掉更糟，因为**他不会把掉帧归因到这个开关上**。

### 关于首屏加载时间为什么三段分开报（v1.0 · T-279）

三段各有各的成因，而合起来那个数没有任何可操作性：

| 段 | 慢了意味着 |
|---|---|
| 解包 | 包大，或者机器 IO 慢 |
| 建场景 | 几何 / 贴图多 |
| 首帧 | 着色器编译——**与材质种类数相关，与面数关系不大** |

客户说「打开要好几秒」时，只有分段的数能回答「该改模型还是该换机器」。

四行的 `limit` 一律是 `—`：**这四个数没有阈值来源**。可接受的首屏时间取决于包多大、
网络在哪一端、客户的耐心，没有一条能写进合同的通用线。报一个编出来的阈值比不报更糟,
它会被当成实测结论引用。

### 关于「贴图显存（估算）」

报告里写的是估算，不是测量。GPU 的实际分配取决于驱动的内部格式、压缩与对齐，
而 WebGL 不暴露这些。这个数字给的是正确的数量级和可靠的相对比较，仅此而已。

### 关于「未测到」这三个字（v1.0 · ADR-0042）

报告里的某一行写着 **未测到（样本 N/8）** 时，它的意思是：那个采样窗口没收到足够的帧，
**这一行没有读数**。它不是「0 fps」，也不是「很慢」。

为什么要专门有这种表述：在此之前，采样窗口一帧样本都没收到时，报告印出来的是

| 指标 | 实测 | 结论 |
|---|---|---|
| 平均帧率 | 0.0 fps | ❌ 超标 |
| P95 帧时间 | 0.0 ms | ✅ 通过 |
| 最慢单帧 | 0.0 ms | ✅ 通过 |

**「0 毫秒的帧时间」被判成通过**——一份看起来是好消息的报告。而这几个数还会往下传：
判 fail 的那行截断爬坡，被截断的档在报告上根本不出现，于是「动态灯上限」四行、
「建议出厂默认阴影档」、「阴影贴图显存」全部跟着错，最后经回填脚本落进
[附件A](附件A_数字资产规范_草案.md) §7 —— 一份合同附件，而它的第一条回填规则是
「数字只来自实测」。

现在的规则：

- 采样窗口到点时样本不足 8 个，**自动延长**，最多延到标称时长的 3 倍；
- 延到上限仍不足，那一行报「未测到」、判 **⚠ 接近上限**（不是通过、也不是超标）；
- 未测到的档**不参与**任何推导：不算上限、不进推荐、不选显存档；
- 爬坡遇到它就收摊——延长到 3 倍还凑不满 8 帧，平均帧时已经超过 75 ms，本来就在红线以下。

**看到「未测到」时该做的事**：去掉 `?fast=1` 重跑一次。仍然未测到，说明这台机器在这个
场景上确实一帧要几百毫秒——那本身就是结论，只是它该由「未测到 + 重跑仍未测到」说出来，
不该由一个假的 0.0 说出来。

### 关于剖切与爆炸这两档（v1.0 · T-281）

这两档量的是**两种完全不同形状的成本**，放在一起报是因为它们都只在「用户点了一下」
的那一刻才出现，而稳态帧率里一点都看不见。

#### 剖切：代价一次性落在切换后的那一帧

three 把裁剪平面的**数量**放进 shader program 的 cache key。于是开一次剖切 =
全场材质重编译一遍，而编译完之后每帧的开销几乎没变。用户的体感是「点一下剖切，
卡一下」——那一下就是这一档的数。

**开与关两次分别量。** 回切也要再编译一遍，复用同一个数字会让这件事从报告上消失，
而用户点两下会遇到两次卡顿。报告里还带着 `renderer.info.programs` 的前后差，它是那两个
毫秒数的成因，不是另一件事。

两行的 `limit` 都是 `—`：**这一档没有阈值来源**。多少毫秒算「卡」取决于机器和场景
复杂度，编一个数比不报更糟。

#### 爆炸：量的是进行中，不是终态

终态与静止画面没有区别——量它会量出一个和没爆炸时一模一样的数，然后被读成「爆炸不费
性能」。动画期间每一帧都要重算成员的世界矩阵并重传，那才是这一档的成本。

#### 场景里没有剖切平面 / 爆炸分组时

报「不适用（本场景没有…）」，**而不是省掉这一档，也不是报 0**。一份少了一整档的报告
与一份「这一档不适用」的报告，在读者眼里是两件事，而只有后者能被信任。

#### 有剖切平面、但都是关着的（v1.0 · ADR-0042）

这是**第三种情形**，与上面两种都不一样，报的是「未测到（剖切平面都是关的）」并判
⚠ 而不是 ✅。

按 [ADR-0039](adr/0039-剖切面的启用判定用世界可见性.md)，世界不可见的刀不装裁剪平面。
于是一份带着一把关着的刀的资产，会让这一档**一条平面都装不上**——而在 ADR-0042 之前，
它产出的是三行绿色「通过」的 `0 ms`，读者据此得出「剖切几乎免费」，与事实相反。

带一把关着的刀是**合法文档**，客户送检的资产照样会这样。看到这一行时：把要验收的那把刀
打开（或让它的祖先可见）后重测。

### 软件渲染

检测到软件渲染时，报告顶部会强制加一段警告，且导出的 Markdown 里也带着：

> ⚠️ 本次为软件渲染（未启用硬件加速或无独立显卡）。帧率类数据**不可作为验收依据**，
> 仅可用于同一台机器上不同模型之间的相对比较。

这段话删不掉，是刻意的（`e2e/tests/bench.spec.ts` 有一条断言盯着它）。

导出的 JSON 里也写着 `capability.level`，**回填脚本据此拒绝把软渲报告写进附件A**。
只写在 Markdown 的那段中文里的话，脚本只能去正则匹配一段措辞——而措辞一改，拒绝就
失效了。

---

## 拿到机器之后的 30 分钟（v1.0 · T-291）

> **这一节是写给「从没参与过这个项目的人」的。** 它的验收标准就是这句话：把这一页打印出来
> 交给现场的同事，他应当能独立跑完一台机器，并交回一个我们的脚本认得的文件。
>
> 如果你照着做卡在了某一步——**那是这一页的问题，不是你的问题**，请把卡住的那一步告诉我们。

### 0 · 你需要什么

| 东西 | 说明 |
|---|---|
| 一台目标机器 | 见下面「三台机器分别是什么」 |
| Chrome 或 Edge | 版本不限。**不要用 IE、不要用 360 兼容模式** |
| 两个 `.w3p` 文件 | 我们随本手册一起给你。文件名就是场景名，别改 |
| bench 页面地址 | 形如 `http://<部署地址>/bench.html`。部署方式见 [DEPLOY.md](DEPLOY.md) |

全程**不需要**装 Node、不需要命令行、不需要联网。第 4 步之后把文件发回来就行。

### 1 · 先确认硬件加速是开着的（**这一步不能跳**）

地址栏输入 `chrome://gpu`（Edge 是 `edge://gpu`），回车。看最上面那张表的第一行：

| 你看到的 | 意味着 | 怎么办 |
|---|---|---|
| `WebGL: Hardware accelerated` | 好，继续 | — |
| `WebGL: Software only, hardware acceleration unavailable` | **这台机器现在测不了** | 见下面两行 |
| 整页打不开 | 浏览器版本太老 | 换 Chrome / Edge |

关着的话，两条路：

1. 设置 → 系统 → 打开「使用图形加速功能（如果可用）」→ **完全退出浏览器再重开**（关窗口不算）；
2. 打开之后仍然显示 software，说明这台机器就是没有可用显卡——**那它就是 M3**，照样测，
   但要在交回文件时说明「这台是关硬件加速的那一档」。

> **为什么这一步不能跳**：软件渲染下的帧率和真实显卡没有可比性。我们的回填脚本会
> **拒绝**这类报告（退出码 1，不是警告），因为那个数字一旦进了合同附件，将来就会被人引用。

### 2 · 三台机器分别是什么

| 档位 | 这台机器是 | 它决定什么 |
|---|---|---|
| **M1** | 客户实际使用的**主力机型** | 验收依据。合同里承诺的数字来自它 |
| **M2** | 客户机型里**配置最低**的那一台 | 我们敢承诺什么。**它是下限，不是平均** |
| **M3** | 一台**关掉硬件加速**的机器 | 最坏情况下用户看到什么 |

三台是三次独立的操作，**不要在一台机器上改配置冒充另一台**。M3 可以是 M1 或 M2 那台机器
关掉硬件加速之后再跑一遍——那是同一台硬件的两种状态，是合理的；改分辨率、改电源模式
来冒充「低配」不是。

### 3 · 每台机器跑两个场景

| 场景 | 文件 | 为什么是它 |
|---|---|---|
| **标准件** | `泵组样板.w3p` | 三台机器跑的是**同一份**，所以三份报告之间可以直接比 |
| **实景** | `<客户场景>.w3p` | 客户真正要用的那一份。它决定我们敢不敢签 |

只跑标准件的话，我们知道三台机器谁快谁慢，却不知道客户的场景在上面跑不跑得动；
只跑实景的话，三台机器的数字互相之间没有基准。**两个都要。**

每个场景的操作是一样的：

1. 打开 bench 页面；
2. 把 `.w3p` 拖到页面上（或点上方按钮选文件）；
3. **等它自己跑完**。期间相机会自动环绕，屏幕上会一直显示「正在测量…」。
   **不要切走标签页、不要锁屏、不要动鼠标去点别的地方**——浏览器会给后台标签页降帧，
   那会让这次测量的数字整体偏低；
4. 跑完之后页面下方出现「下载 JSON」按钮，点它。

> **一台机器上两个场景 = 两个 JSON 文件。** 三台机器一共六个。

### 4 · 文件怎么命名

下载下来的文件名形如 `bench-2026-08-11T03-20-11-123Z.json`，**要改名**。规则一句话：

```
<档位>-<这台机器的名字>-<场景>-<日期>.json
```

例：

```
M1-操作站-标准件-20260811.json
M1-操作站-实景-20260811.json
M2-车间工控机-标准件-20260811.json
M2-车间工控机-实景-20260811.json
M3-操作站关加速-标准件-20260811.json
M3-操作站关加速-实景-20260811.json
```

**开头那三个字符必须是 `M1-` / `M2-` / `M3-`**，脚本按它认档位；后面怎么写都行，
中文可以，只要别带 `/ \ : * ? " < > |` 这几个字符。

> ⚠ **本节的命名与任务卡 T-291 的字面写法（`<代号>-<日期>.json`）不同，以本节为准。**
> 理由是已经落地的 `scripts/apply-bench-report.mjs` 按 `^(M[123])-(.+)$` 解析文件名——
> 一份叫 `操作站-20260811.json` 的报告它认不出来。**手册要产出脚本吃得下的东西**，
> 否则「合规命名」四个字就没有落点。

### 5 · 交回来

把那六个 `.json` 发回给我们就完了，**你这边的活到此为止**。

我们这边做的是（写在这里只是让你知道后面发生了什么）：

```bash
# 1 · 文件放进仓库
cp *.json docs/bench-reports/

# 2 · 回填合同附件 A §7
node scripts/apply-bench-report.mjs

# 3 · 从此这张表被逐字节盯着，手改一个字就红
node scripts/apply-bench-report.mjs --check
```

### 6 · 什么算通过

**四条程序性硬门槛**——这四条是脚本判的，不是人判的，不满足就是没通过：

| # | 门槛 | 谁在判 |
|---|---|---|
| 1 | 报告不是软件渲染（`capability.level !== "software"`） | `apply-bench-report.mjs` 直接拒绝，退出 1 |
| 2 | 报告格式版本是 1，且四个指标行都在（**跑完了**） | 同上，缺哪一行会点名 |
| 3 | 每一档硬件**恰好一份**报告，档位不重复、不插值 | 同上（§7 回填规则 5） |
| 4 | 回填之后 `--check` 绿 | `pnpm verify` 里一直在跑 |

⚠ **M3 是唯一的例外**：它按定义就是软件渲染，所以它**不进附件A §7**。它的用途是回答
「最坏情况下用户看到什么」，结论写进 [IMPL_NOTES.md](IMPL_NOTES.md)，不写进合同。

**商务建议线**——这几条是给人看的，**不是硬门槛**，不满足也不会有任何脚本变红：

| 指标 | 绿 | 黄 | 红 |
|---|---|---|---|
| 平均帧率 | ≥ 45 fps | 25 ~ 45 fps | < 25 fps |
| P95 帧时间 | ≤ 33 ms | 33 ~ 66 ms | > 66 ms |
| 首屏 · 合计 | ≤ 3 s | 3 ~ 8 s | > 8 s |

黄灯不是「不能签」，是「签之前先说清楚客户会看到什么」。**红灯要先减面或降档再谈**。

### 7 · 常见问题

| 现象 | 多半是 |
|---|---|
| 拖进去没反应 | 文件不是 `.w3p`，或者拖到了页面外面的空白区 |
| 跑到一半页面白了 | 显存不够。记下来告诉我们，**这本身就是一条结论** |
| 报告顶部有一段红色警告 | 硬件加速没开，回第 1 步 |
| 某几行写着「未测到」 | 正常。见本页「关于「未测到」这三个字」一节——**它不是失败，是没量到** |
| 两次跑同一个场景数字不一样 | 正常，差 10% 以内不用管；差一倍请重跑一次并都发回来 |

---

## 当前状态

**尚无目标机器实测数据。** 下面这份是**开发机**的记录，用途只有一个：证明这几档确实
跑得起来、报告确实出得来。**不可作为验收依据**，理由就是本页开头那三条。

### 开发机一轮（2026-08-01）

| 项 | 值 |
|---|---|
| CPU | Intel Core i9-14900HX |
| 内存 | 34 GB |
| 系统 | Windows 11 |
| 渲染 | **SwiftShader 软件渲染**（CI 与本仓库开发环境均无硬件加速） |
| Player 体积 | gzip **243.0 KB / 400 KB**（`pnpm size`，与显卡无关，**这一项可信**） |
| 帧率类 / 灯光档 / 阴影档 | **未记录数字** |

帧率、承载上限、动态灯上限这几档**故意留空而不是填软渲数字**。软件渲染下的帧率与
真实 GPU 无可比性，填进去只会变成一个将来被人引用的错数字——
「一份带错数字的合同附件，比一份留白的危险得多」这句话对这里同样成立。

`pnpm size` 那一行是唯一与显卡无关的量，所以它是这一轮唯一被记下来的数字。

本仓库的开发与 CI 环境只有 SwiftShader 软件渲染，帧率数据无参考价值，因此
[附件A](附件A_数字资产规范_草案.md) §7 的性能指标表**留空**而不是用软渲数据填充。
一份带错数字的合同附件，比一份留白的危险得多。

拿到目标机器后，回填这条路是**脚本走的**（v1.0 · T-280），不是手抄：

1. 在目标机器上跑一遍本页面，点「下载 JSON」；
2. 把文件放进 `docs/bench-reports/`，命名 `<档位>-<代号>.json`——
   档位取 **M1 / M2 / M3**（对应下面「建议至少测三种配置」的三行），代号是这台机器
   在合同里的称呼，例如 `M2-车间工控机.json`；
3. `node scripts/apply-bench-report.mjs`；
4. 提交。`pnpm verify` 里的 `--check` 从此盯着这张表**逐字节**——手改一个字就红。

两条脚本里写死的规则，值得在这里重复一遍：

- **软渲报告会被拒绝**（退出 1，不是警告）。软渲帧率与真实 GPU 没有可比性，填进合同
  附件就是一个将来会被引用的错数字。
- **一档硬件一份报告，不做插值**（§7 回填规则 5）。同一档位放两份文件会直接报错，
  而不是挑一份或者取平均。

四行全部变成 `[实测]` 之后，此段删除。

### 建议至少测三种配置

| 配置 | 为什么 |
|---|---|
| 客户实际使用的主力机型 | 验收依据 |
| 客户机型里配置最低的一台 | 决定我们敢承诺什么 |
| 一台关闭硬件加速的机器 | 摸清最坏情况下用户看到什么 |
