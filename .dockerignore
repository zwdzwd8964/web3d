# 注意：e2e/ 、tools/ 、test/ 都不能排除 —— pnpm-workspace.yaml 把它们列为
# workspace 成员，目录缺失会让 --frozen-lockfile 判定 lockfile 不一致。
node_modules
**/node_modules
.git
.gitignore
.github

dist
**/dist
coverage
**/coverage
*.tsbuildinfo

# 发布包样例：`packages/player/public` 下躺着 golden.w3p（4,555 B）与 gp2.w3p（84,681 B），
# 它们是 E2E 的夹具，不是要发出去的内容。`COPY . .` 会把它们带进镜像并被 nginx 从
# /player/ 下服务出去——一份谁都能下载的示例文档，没人会注意到它在那里。
**/public/*.w3p

test-results
playwright-report
blob-report
.playwright

.vscode
.env
.env.*
